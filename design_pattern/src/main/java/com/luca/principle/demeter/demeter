迪米特法则（最少知道原则）：
    介绍：
        1：对自己依赖的类，知道越少越好
        2：只和直接朋友通信
        直接朋友：被依赖的类，在依赖的类中只是成员变量、方法的输入、方法返回值。
    注意和细节：
        1）迪米特法则的核心是降低类之间的耦合
        2）由于每个类的减少了不必要的依赖，因此迪米特法则只是要求降低类间（对象间）
        耦合关系，并不是要求完全没有依赖关系